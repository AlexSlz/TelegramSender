//alert(window.location)
chrome.runtime.onMessage.addListener((msgObj) => {
  if (msgObj === 'start') {
  }
})
//document.querySelectorAll('img').forEach((img) => {
//  if (img.parentNode != null)
//    img.parentNode.innerHTML += `<a href="${img.src}" class="alex-btn">Download</a>`
//})
